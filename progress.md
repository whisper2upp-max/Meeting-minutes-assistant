# 项目进度档案（progress.md）

> 更新时间：2026-08-29 深夜。本文件完整记录需求、规划、当前状态、遇到的问题与全部尝试，供接续开发/排查使用。

---

## 一、用户需求（原话与演进）

1. **初始需求**：公司主用 Teams、无 AI 纪要功能、禁用各类 AI 软件网站、仅认证百炼 API。希望做一个程序：开会录音（戴耳机、**内录会议音频**+麦克风）→ 录完转文字（最好按音色区分说话人）→ 生成会议纪要。转写与纪要必须走百炼 API。
2. **技术指定**：Windows / macOS 双平台；转写 **fun-asr**；纪要 **qwen-flash**；支持公司专属网关（ws-*.maas.aliyuncs.com）与官方端点（dashscope.aliyuncs.com）。
3. **形态要求**："我不想只是个 python 脚本"——要双击即用的 **.app / .exe**；界面"好好设计"。
4. **零配置目标（当前主目标）**："太复杂了，又要装驱动，又要开始前启动脚本。用户需要的是方便，**一次性安装程序，其他都内置在程序里，打开就能直接用**。"

## 二、总体规划与落地情况

| 里程碑 | 内容 | 状态 |
|---|---|---|
| M1 云链路 | 百炼临时存储上传 → fun-asr 异步转写（diarization）→ qwen-flash 纪要，断点续跑 | ✅ 真机端到端通过（TTS 双音色 53 秒对话正确分出说话人1/2，纪要结构完整） |
| M2 录音 | Win WASAPI loopback（免驱动）/ macOS BlackHole+麦克风，RMS 归一混单声道 16k | ✅ 代码完成，macOS 录音被权限问题卡住（见下） |
| M3 界面 | pywebview + 单文件 HTML（步骤条/大录音按钮/设置弹窗/日志/结果卡），Tkinter 兜底 | ✅ |
| M4 打包 | PyInstaller：macOS .app（图标、内置驱动 pkg、注入麦克风声明）+ zip；GitHub Actions 出 Windows exe | ✅（macOS 麦克风授权链路见"当前问题"） |
| M5 零配置 | 驱动内置自动安装（一次管理员密码）、录音时自动建多输出设备/结束还原、换耳机自适应 | ✅ 实现并真机验证；**最终用户录音验收未通过** |
| M6 真实验收 | 用户在 app 内录一次真实双轨并出纪要 | ❌ **当前唯一卡点** |

## 三、当前卡点（唯一未闭环项）

**macOS 麦克风权限（TCC）链路反复失败，用户三次真实验收均未能录到声音。**

### 现象时间线（含产物证据）

| 时间 | 事件 | 证据 |
|---|---|---|
| 19:52 | 用户旧版界面录音：第三次尝试 mic 流可开，录 0.1 分钟，云端报 ASR_RESPONSE_HAVE_NO_WORDS（无语音） | 会话 2026-08-29_1952* |
| 20:45 | **用户第一次正式验收**：自动切"会议内录输出"成功、BlackHole 轨开了 237 秒但**绝对零值（RMS=0.00000）**、麦克风轨根本没打开；用户关窗，录音被静默丢弃（缺陷） | 会话 2026-08-29_2045：仅 track_system.wav，无 track_mic.wav/audio.wav |
| 20:53 | 用户第二次验收（加守卫后的新版）：立即报"麦克风无法打开"；**系统设置-隐私-麦克风列表里根本没有本 app** | 用户截图（错误面板完整出现） |
| 21:0x | 注入 NSMicrophoneUsageDescription 后用户第三次验收：**授权弹窗终于弹出**（截图可见"想要访问麦克风"弹窗），但弹窗悬空未点时程序已抢先报错（竞态） | 用户截图（弹窗+错误面板同框） |
| 最新 | 加 13 秒授权等待重试后（1ec2e2c），用户反馈"还是不行"，**尚未拿到具体报错文字/截图** | 待补充 |

### 根因分析（已确认部分）

1. **打包缺 `NSMicrophoneUsageDescription`**：PyInstaller 默认不写。macOS 对缺此声明的 app **静默拒绝麦克风：不弹框、不进隐私列表、输入流拿到全零数据**。→ 已修复（2eb1c8f，PlistBuddy 注入 + ad-hoc 重签），**修复后弹窗确实出现了**（用户截图证实）。
2. **授权弹窗与打开流的竞态**：弹窗悬空时流打开立刻失败，程序随即报错。→ 已修复（1ec2e2c，13 秒重试窗口 + 报错带底层原因）。
3. **ad-hoc 签名不稳定（已知隐患）**：每次重新打包 app 签名即变，macOS 可能把新二进制当"新 app"要求重新授权。给同事分发不受影响（每人一次授权），但**开发期每次重建都可能再弹一次**。
4. **助手环境限制**：我的沙箱进程被 TCC 静默置零（实测录音 RMS=0.0000），**无法代替用户做真实录音测试**，只能靠用户实测反馈。

### 下一步排查（按优先级）

1. **拿到最新失败的报错全文**（新版报错会带底层原因，如 `Error opening InputStream: ...`），据此定位：
   - 仍是权限 → 确认用户是否**完全退出（Cmd+Q）后重开**的新版；检查隐私列表是否已出现本 app 且开启
   - PortAudio 设备错误（采样率/独占） → 针对性修
   - 能录但全零 → 签名又变导致授权失效，重新授权一次
2. 若 TCC 链路继续反复：考虑 **ScreenCaptureKit 方案**（macOS 13+ 系统音频采集走"屏幕录制"权限，避开麦克风 TCC 对输入流的置零；工程量约 1-2 天，需 Swift/PyObjC 桥接）
3. Windows 侧不受影响（WASAPI loopback 无此权限模型），可直接分发 exe 验证

## 四、已完成的全部尝试与结果

### 云端链路（全部通过）
- 百炼专属网关探测：compatible-mode LLM ✓、/api/v1/uploads 临时存储 ✓（curl + SDK 双验证）
- 发现并解决：macOS 系统代理（Clash 127.0.0.1:7897）拦截 Python 的 HTTPS（curl 不受影响）→ 包入口对 `aliyuncs.com` 全局 NO_PROXY 绕过
- fun-asr 说话人分离：TTS 双音色（Grandma/Rocko）53 秒对话 → 说话人1/2 正确分离；Tingting+Reed 音色过近会被并成一人（真人差异更大，预期无碍）
- 端点切换：官方 ↔ 专属网关均为配置项，端到端各验证一遍（会话 1900/1849 可查）
- 成本实测口径：fun-asr ≈0.79 元/小时，qwen-flash 每场 <1 分钱

### 录音与系统配置（macOS）
- BlackHole 安装：brew 因沙箱无法 sudo → 改 osascript 管理员授权框装 pkg ✓；killall coreaudiod 后设备出现 ✓
- 多输出设备：CoreAudio ctypes 直调（常量真机核对，两个 FourCC 与记忆不符，已修正）→ 创建/切换/还原/幂等全部真机验证 ✓
- **驱动内置踩坑**：先内置驱动目录 → PyInstaller 把数据里的 Mach-O 过滤掉 → 装出"空壳"驱动（codesign: not signed、Contents/MacOS 为空、coreaudiod 不加载）→ **改内置官方 pkg + /usr/sbin/installer**（单文件不受 PyInstaller 影响）→ 从零首装链路真机通过 ✓
- app 内自动验收模式（MEETINGKIT_AUTOTEST 环境变量）：确认了 TCC 置零与授权失效问题

### 程序缺陷修复记录
| 缺陷 | 修复 |
|---|---|
| 用户关窗静默丢弃录音 | webui 加 closing 事件确认拦截 |
| 麦克风开不了仍继续录（录出全零浪费几分钟） | 麦克风为必需轨，失败立即报错（后改为 13 秒重试） |
| 转写云端错误码生硬（ASR_RESPONSE_HAVE_NO_WORDS 等） | 错误码→可操作中文提示映射 |
| 录音前需要手动跑配置脚本 | 录音 worker 自动 prepare/restore，脚本退化为可选工具 |
| Info.plist 缺麦克风声明（静默拒绝根因） | PlistBuddy 注入 + ad-hoc 重签（构建脚本与 CI 均已加） |

### 测试与产物
- 单元测试 22 项全过（混音/归一/配置/管线缓存/prompt/mac_setup）
- `dist/会议纪要助手.app`（含图标、内置 pkg、麦克风声明）+ `dist/会议纪要助手-macOS.zip`
- `.github/workflows/build.yml`：Windows exe / macOS app 双产物
- git 已提交至 1ec2e2c；`百炼.md`（API Key）已被 .gitignore 排除并二次验证

## 五、复现与构建速查

```bash
cd /Users/cici/Desktop/AI/project/会议纪要
.venv/bin/python -m pytest                 # 22 项测试
./scripts/build_macos.sh                   # 重建 .app（含麦克风声明注入）
# 用户验收：Cmd+Q 退出旧实例 → 打开 dist/会议纪要助手.app → 点录音 →（若弹授权框点「好」，
#   程序会等 13 秒）→ 播放桌面「内录测试音频.wav」+ 说两句话 → 停止 → 看日志"录音结束：2 轨"
```

## 六、结论

- **产品目标（免驱动安装、免脚本、打开即用）的实现已全部落地并逐项验证**；
- **唯一未闭环**：macOS 麦克风授权的用户实测（连续三次被 TCC 权限链路挡住，两个根因已修复，最新一次结果待用户反馈）；
- 助手侧无法代测（沙箱被 TCC 置零），此项验收只能由用户完成；若继续反复，备选方案为 ScreenCaptureKit 改造（见"下一步排查"）。
