# 更新日志

本项目遵循语义化版本。日期使用 `YYYY-MM-DD` 格式。

## 0.1.7 — 2026-09-04

### 新增

- 录制会议和导入音视频时可选择“简要、标准、详细”三档纪要。
- 标准档补充议题背景、主要观点和讨论结论；详细档进一步保留讨论脉络、关键数据、分歧和决策依据。
- 详细档在默认 `qwen-flash` 配置下自动升级为 `qwen-plus`；用户手动选择的其他模型保持不变。
- 纪要编辑页可直接切换档位并基于已有完整转写重新生成，不重复上传音频或执行转写。
- 重新生成前自动将当前纪要备份到本场会议的 `纪要历史版本` 文件夹。
- 参会人输入改为可增删的头像标签，支持粘贴多行，并可在设置中维护常用参会人。
- 说话人匹配界面直接展示常用参会人候选按钮，结合样例发言完成可视化匹配。

### 改进

- 为每场会议增加 `session.json`，持久化会议标题、参会人、纪要详细程度和实际生成模型；旧会话自动按简要档兼容。
- 管线进度会同步更新界面阶段，避免处理期间长时间停留在“正在准备音频”。

## 0.1.6 — 2026-08-31

### 新增

- 纪要编辑页新增“匹配参会人”：根据每位说话人的样例发言，后期填写或选择真实姓名。
- 每场会议的匹配结果独立保存为 `speaker_map.json`；保存后从原始 `transcript.json` 重新生成带姓名的完整转写。
- 将纪要正文里仍存在的“说话人N”标签替换为确认姓名，同时保留用户的其他人工编辑。
- Windows 麦克风测试增加实时输入音量条。

### 修复

- 麦克风列表明确分为“自动选择”和“固定设备”，始终显示置顶的系统默认项，并保留当前默认物理设备供固定选择。
- 系统默认项会继续跟随 Windows 默认输入及耳机插拔自动切换，不再因隐藏同名物理项造成误解。
- 麦克风测试改用独立的阻塞读取/播放链路，优先使用 Windows 当前全局默认输出，并增加适度回放增益，修复测试启动但听不到耳返的问题。
- 测试链路与正式会议录制继续隔离，不改动已经稳定工作的双轨录制实现。

## 0.1.5 — 2026-08-31

### 新增

- Windows 录制页新增麦克风实时测试：将当前选中麦克风低延迟回放到 Windows 当前默认耳机或扬声器。
- 测试状态明确显示实际输入与实际播放设备；离开页面、开始录音或退出应用时自动释放测试流。

### 修复

- “跟随 Windows 默认”现在显示当前真实麦克风名称，并隐藏未被固定选择的同名默认物理项，避免看起来多出一个重复设备。
- 修复 Windows“打开输出目录”错误使用 Explorer `/select` 参数，导致只打开自定义目录的父级（常见为 Documents）。
- 打开目录失败时不再静默忽略，界面会显示可操作的错误信息。

## 0.1.4 — 2026-08-30

### 修复

- 修复 Windows 选择“系统默认麦克风”时可能误用旧的 Sound Mapper 端点，导致录音无人声。
- 麦克风列表只保留真实的 WASAPI 输入设备，排除 MME、DirectSound、输出回环与同名重复项。
- 默认麦克风直接跟随 Windows WASAPI 默认输入端点。
- 按麦克风支持的原生声道数打开录音流，再降混为转写所需的单声道，兼容双声道麦克风阵列。
- 当旧配置指向已拔出设备或 Windows 未设置默认输入时，给出明确的操作提示。

## 0.1.3 — 2026-08-30

### 修复

- 修复 Windows 结束会议录音后程序可能直接退出的问题。
- 将 WASAPI 回环与麦克风从阻塞式 `stream.read()` 读线程改为 PyAudio 回调采集，避免关闭流时与原生读取并发。
- 结束时先停止并关闭全部原生音频流，再排空写盘队列、封口 WAV 文件并释放 PyAudio。
- 补充回调录音、双声道降混与资源关闭顺序的回归测试。

## 0.1.2 — 2026-08-30

### 修复

- 修复 Windows 会议录制启动时报 `Stream.__init__() got an unexpected keyword argument 'as_loopback'`。
- 按 PyAudioWPatch 的正式接口直接打开默认输出设备对应的 WASAPI 回环输入，不再传入其他音频库的参数。
- 将 Windows 录音测试替身改为显式的 PyAudio 参数签名，防止未知关键字参数再次绕过测试。

## 0.1.1 — 2026-08-30

### 修复

- Windows 版改用系统 CryptoAPI 信任证书库，兼容公司统一下发的根证书与中间证书。
- Windows 录制页明确显示“默认输出设备（WASAPI 自动内录）”；检测不到回环设备时在录制前直接提示，不再静默退化为仅麦克风。
- 保持严格 HTTPS 校验，不通过关闭证书验证绕过连接问题。
- 为上传、转写、结果下载和纪要生成补充可操作的证书错误提示。
- Windows 与 macOS 构建在打包前执行完整测试；Windows 打包显式收集系统证书后端。

## 0.1.0 — 2026-08-29

首个可用版本。

### 新增

- 支持 macOS 和 Windows 的会议内声、麦克风双轨录制。
- 支持导入常见音频和视频文件生成会议纪要。
- 接入百炼 fun-asr，自动转写并区分说话人。
- 接入 qwen-flash，生成摘要、关键决策和行动事项。
- 提供历史会议列表、所见即所得纪要编辑、Markdown 保存与导出。
- 提供 Weekflow 风格的会议工作台、处理进度和运行日志。
- 在应用首页提供使用说明和版本更新日志。

### 隐私与存储

- 音频、转写与会议纪要默认保存在用户指定的本机输出目录。
- API Key 仅保存在本机配置文件中。
- 转写时音频会临时上传到用户配置的百炼服务端点。

### 体验修复 — 2026-08-30

- 修复长篇纪要和历史会议列表无法独立滚动的问题。
- 补全 Markdown 一级至六级标题选项。
- 支持在编辑器中增加或删除表格行列。
- 修复表格保存后因行间空行而无法被 Markdown 预览器识别的问题。
- 支持从历史记录永久删除完整会议文件夹，并在删除前明确提示影响范围。
- 缩小录音控制台，将处理状态与步骤移动到录音控制台上方。
- 导入生成页同步采用上方状态面板与紧凑处理说明卡，保持与录制页一致。
- 统一会议录制与导入生成页的双栏宽度、间距和顶部基线，消除页面切换时的横向跳动。
