# 更新日志

本项目遵循语义化版本。日期使用 `YYYY-MM-DD` 格式。

## 0.1.2 — 2026-08-30

### 修复

- 修复 Windows 会议录制启动时报 `Stream.__init__() got an unexpected keyword argument 'as_loopback'`。
- 按 PyAudioWPatch 的正式接口直接打开默认输出设备对应的 WASAPI 回环输入，不再传入其他音频库的参数。
- 将 Windows 录音测试替身改为显式的 PyAudio 参数签名，防止未知关键字参数再次绕过测试。

## 0.1.1 — 2026-08-30

### 修复

- Windows 版改用系统 CryptoAPI 信任证书库，兼容公司统一下发的根证书与中间证书。
- Windows 录制页明确显示“默认输出设备（WASAPI 自动内录）”；检测不到回环设备时在录制前直接提示，不再静默退化为仅麦克风。
- 保持严格 HTTPS 校验，不通过关闭证书验证绕过连接问题。
- 为上传、转写、结果下载和纪要生成补充可操作的证书错误提示。
- Windows 与 macOS 构建在打包前执行完整测试；Windows 打包显式收集系统证书后端。

## 0.1.0 — 2026-08-29

首个可用版本。

### 新增

- 支持 macOS 和 Windows 的会议内声、麦克风双轨录制。
- 支持导入常见音频和视频文件生成会议纪要。
- 接入百炼 fun-asr，自动转写并区分说话人。
- 接入 qwen-flash，生成摘要、关键决策和行动事项。
- 提供历史会议列表、所见即所得纪要编辑、Markdown 保存与导出。
- 提供 Weekflow 风格的会议工作台、处理进度和运行日志。
- 在应用首页提供使用说明和版本更新日志。

### 隐私与存储

- 音频、转写与会议纪要默认保存在用户指定的本机输出目录。
- API Key 仅保存在本机配置文件中。
- 转写时音频会临时上传到用户配置的百炼服务端点。

### 体验修复 — 2026-08-30

- 修复长篇纪要和历史会议列表无法独立滚动的问题。
- 补全 Markdown 一级至六级标题选项。
- 支持在编辑器中增加或删除表格行列。
- 修复表格保存后因行间空行而无法被 Markdown 预览器识别的问题。
- 支持从历史记录永久删除完整会议文件夹，并在删除前明确提示影响范围。
- 缩小录音控制台，将处理状态与步骤移动到录音控制台上方。
- 导入生成页同步采用上方状态面板与紧凑处理说明卡，保持与录制页一致。
- 统一会议录制与导入生成页的双栏宽度、间距和顶部基线，消除页面切换时的横向跳动。
