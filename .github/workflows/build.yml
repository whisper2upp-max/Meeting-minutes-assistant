name: build-app

# 手动触发，或推送到 main 时自动构建
on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  windows:
    name: 打包 Windows exe
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: 安装依赖
        run: pip install -r requirements.txt pyinstaller pytest
      - name: 运行测试
        run: python -m pytest
        env:
          PYTHONPATH: src
      - name: 打包 exe
        run: >
          pyinstaller --noconfirm --clean --windowed --onefile
          --name "会议纪要助手"
          --paths src
          --add-data "src\meetingkit\webui\index.html;meetingkit\webui"
          --add-data "src\meetingkit\webui\styles.css;meetingkit\webui"
          --add-data "src\meetingkit\webui\app.js;meetingkit\webui"
          --hidden-import meetingkit.audio.windows_rec
          --hidden-import pyaudiowpatch
          --hidden-import truststore._windows
          --hidden-import webview.platforms.winforms
          --hidden-import webview.platforms.edgechromium
          --version-file assets\version_info.txt
          --icon assets\app.ico
          run_app.py
      - name: 上传产物
        uses: actions/upload-artifact@v4
        with:
          name: meetingkit-windows-exe
          path: dist/会议纪要助手.exe
          if-no-files-found: error

  macos:
    name: 打包 macOS app
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: 安装依赖
        run: pip install -r requirements.txt pyinstaller pytest
      - name: 运行测试
        run: python -m pytest
        env:
          PYTHONPATH: src
      - name: 打包 app
        run: |
          pyinstaller --noconfirm --clean --windowed \
            --name "会议纪要助手" \
            --paths src \
            --add-data "src/meetingkit/webui/index.html:meetingkit/webui" \
            --add-data "src/meetingkit/webui/styles.css:meetingkit/webui" \
            --add-data "src/meetingkit/webui/app.js:meetingkit/webui" \
            --add-data "assets/BlackHole2ch-0.7.1.pkg:meetingkit/assets/" \
            --hidden-import meetingkit.audio.macos_rec \
            --hidden-import webview.platforms.cocoa \
            --icon assets/app.icns \
            --osx-bundle-identifier com.meetingkit.app \
            run_app.py
          /usr/libexec/PlistBuddy -c "Set :CFBundleShortVersionString 0.1.7" "dist/会议纪要助手.app/Contents/Info.plist" 2>/dev/null || /usr/libexec/PlistBuddy -c "Add :CFBundleShortVersionString string 0.1.7" "dist/会议纪要助手.app/Contents/Info.plist"
          /usr/libexec/PlistBuddy -c "Set :CFBundleVersion 0.1.7" "dist/会议纪要助手.app/Contents/Info.plist" 2>/dev/null || /usr/libexec/PlistBuddy -c "Add :CFBundleVersion string 0.1.7" "dist/会议纪要助手.app/Contents/Info.plist"
          /usr/libexec/PlistBuddy -c "Add :NSMicrophoneUsageDescription string 会议声音录制需要访问麦克风，用于录入您自己的发言" "dist/会议纪要助手.app/Contents/Info.plist"
          codesign --force --deep --sign - "dist/会议纪要助手.app"
          zip -q -r -y "dist/会议纪要助手-macOS.zip" "dist/会议纪要助手.app"
      - name: 上传产物
        uses: actions/upload-artifact@v4
        with:
          name: meetingkit-macos-app
          path: dist/会议纪要助手-macOS.zip
          if-no-files-found: error
